#include <stdio.h>
int main() {
    float num1, num2;
    scanf("%f %f", &num1, &num2); // Reads two float numbers
    printf("Product: %.2f\n", num1 * num2); // Prints the product of the numbers with two decimal places
    return 0;
}

    
